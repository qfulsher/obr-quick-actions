import { Routes } from '@angular/router';
import { MainPopoverComponent } from './components/main-popover/main-popover.component';

export const routes: Routes = [
    {
        path: '', 
        component: MainPopoverComponent
    }
];
